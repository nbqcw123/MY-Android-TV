name: Build Release

on:
  push:
    tags:
      - "v*.*.*"

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    env:
      GRADLE_OPTS: "-Dorg.gradle.daemon=false -Dorg.gradle.parallel=true -Dorg.gradle.jvmargs=-Xmx4096m"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: "zulu"
          java-version: "17"
          cache: "gradle"

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Configure Gradle mirror
        run: |
          mkdir -p ~/.gradle
          cat > ~/.gradle/init.gradle << 'EOF'
          allprojects {
              repositories {
                  maven { url 'https://maven.aliyun.com/repository/google' }
                  maven { url 'https://maven.aliyun.com/repository/central' }
                  maven { url 'https://maven.aliyun.com/repository/gradle-plugin' }
                  maven { url 'https://maven.aliyun.com/repository/public' }
              }
          }
          settingsEvaluated { settings ->
              settings.pluginManagement {
                  repositories {
                      maven { url 'https://maven.aliyun.com/repository/google' }
                      maven { url 'https://maven.aliyun.com/repository/central' }
                      maven { url 'https://maven.aliyun.com/repository/gradle-plugin' }
                      maven { url 'https://maven.aliyun.com/repository/public' }
                  }
              }
          }
          EOF

      - name: Build APK
        timeout-minutes: 30
        run: |
          chmod +x ./gradlew
          ./gradlew assembleDebug --no-daemon --continue 2>&1 | tail -20
          echo "=== Build output ==="
          find app/build -name "*.apk" 2>/dev/null || echo "No APKs found in app/build"
          ls -laR app/build/outputs/ 2>/dev/null || echo "No outputs dir"

      - name: Check APK output
        run: |
          echo "=== Checking APK output ==="
          find app/build -type f -name "*.apk" 2>/dev/null || echo "No APKs found"

      - name: Get version
        id: version
        run: echo "version=${GITHUB_REF#refs/tags/v}" >>$GITHUB_OUTPUT

      - name: Rename APKs
        run: |
          mkdir -p artifacts
          found=0
          for file in app/build/outputs/apk/debug/*.apk; do
            if [ -f "$file" ]; then
              basename=$(basename "$file" .apk)
              mv "$file" "artifacts/MY-Android-TV-${{ steps.version.outputs.version }}-${basename}.apk"
              found=1
            fi
          done
          if [ "$found" -eq 0 ]; then
            echo "❌ No APKs found in app/build/outputs/apk/debug/"
            exit 1
          fi
          ls -la artifacts/

      - name: Upload Release
        uses: ncipollo/release-action@v1
        with:
          name: v${{ steps.version.outputs.version }}
          token: ${{ secrets.GITHUB_TOKEN }}
          omitBodyDuringUpdate: true
          omitNameDuringUpdate: true
          omitPrereleaseDuringUpdate: true
          allowUpdates: true
          artifacts: artifacts/*
